CONTEXT ProjectAdministration IN ENGLISH
-- RJ/20161122: split source, so that the ontology can be easily reused (e.g. by RISMAN).

  INCLUDE "ProjectAdmin.xlsx" -- Default population
  INCLUDE "ProjectAdmin.adl"  -- Ontology: relations, rules etc.
  INCLUDE "ProjectAdmin.ifc"  -- Interfaces (Public, Planner)
  INCLUDE "ProjectLeader.ifc" -- Interfaces (Project leader)

ENDCONTEXT